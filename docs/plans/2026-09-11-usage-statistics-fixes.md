# 第二批网站反馈：U2–U6

用户确认 U2 Playground 已修复，U3 功能板块服务不同需求、保留。授权修复 U4 View 菜单、U5 Settings，并自主改善 U6 Statistics。

## 方案

- U4：列选择菜单按 Radix 可用视口高度限制并内部滚动，保留列勾选行为；长字段名称换行，菜单宽度自适应。
- U5：头像 Settings 改为已有个人设置路由 `/dashboard/setting/updateUser`，补充头像按钮可访问名称。
- U6：保留 Usage、Statistics 两个侧栏入口，解释各自用途并提供互相跳转。Usage 继续提供逐条请求、费用和实时指标；Statistics 展示所选时间范围的请求质量与性能趋势，不混入不受筛选控制的实时 RPM/TPM/今日费用。
- 筛选自动刷新（输入防抖），重置立即恢复默认查询；终止旧请求并防止旧结果覆盖。日期缺失/倒置时明确提示，不展示旧结果；跨天图表标签包含日期。补充区间与图表粒度说明。
- 日期选择弹层也限制视口高度，以便手机和短窗口选择时间。

## 影响与验证

仅前端，不改后端或数据结构。不继续其他编号问题。浏览器验证 View 边界/滚动/勾选、普通用户 Settings 路由、时间/粒度自动查询、重置、快速切换竞争、失败重试、缺失区间、跨天标签和移动布局。TypeScript、定向 lint、独立目录生产构建。

验证完成：上述浏览器场景通过，覆盖 1440px 桌面、1280×600 短窗口和 390px/320px 手机；无页面错误。TypeScript、定向 lint、隔离目录生产构建通过（构建仅有原有未使用图标和 Browserslist 提示）。模拟会话与数据用于检查交互，不代表实际账户统计数据已验收。预览继续运行于 3001 端口；未提交、未推送。
