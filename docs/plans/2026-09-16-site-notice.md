# 网站公告栏

首页导航下方和控制台首页顶部显示同一公告，在系统设置中独立编辑、预览和保存。复用后端现有 Notice 配置及公开 `/api/notice`，不迁移数据，不新增后端业务接口。

公告按纯文本展示，保留换行，长内容可展开；内容为空时隐藏。加载失败不影响其他页面。保存时只更新 Notice，不提交其他系统设置。界面支持中英文，公告正文由管理员填写。

验证公开读取、空公告隐藏、多行展开和手机布局；检查保存 payload、失败处理和类型/生产构建。示例仅用于预览和测试，不自动发布线上公告。
