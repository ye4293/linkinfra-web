# 最后两项反馈：U12 / U14

用户要求处理 U12 和 U14，忽略 U13 首页整体样式建议。

- U12：移除首页顶部伪搜索按钮及配套 `/` 跳转快捷键，保留模型区实际搜索和筛选。
- U14：Usage 使用共享 DataTable；其表头 sticky 原本受内层 Table 的 overflow-auto 容器限制。Table 新增可选容器样式，DataTable 关闭内层滚动，统一在已有外层容器滚动。表头不透明，避免数据文字透出；其他直接使用 Table 的页面保留默认行为。

验证：浏览器实际滚动长表，检查纵向固定、横向列对齐、表尾、切换显示列和展开行；检查首页搜索筛选及桌面/移动布局。TypeScript、定向 lint、生产构建。不改首页整体风格。

结果：TypeScript、定向 lint、隔离生产构建均通过。浏览器滚动检查表头位置不变、横向列对齐，展开行与勾选正常；桌面和 320px 首页搜索可过滤模型，无页面错误。未提交/推送。
