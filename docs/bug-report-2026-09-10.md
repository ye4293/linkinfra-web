# 网站反馈处理记录

来源：用户提供的《网站需求与bug记录.html》，共 18 项。2026-09-11 用户要求逐项讨论，先处理前 5 项。以下方案以本轮意见为准。

| 编号 | 用户意见                              | 本轮实现                                                                                                                                 |
| ---- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| A1   | 不是硬性缺失，可自主优化              | 模型详情添加 Get API key 和 Copy model ID；已有 Key 可直接复用，无需按模型重新创建。                                                     |
| A2   | 登录后直达平台 token 页，未登录去登录 | 首页获取 Key 入口统一使用 `https://platform.linkinfra.ai/dashboard/token`；未登录使用平台 `/sign-in` 并携带回到 token 页的 callbackUrl。 |
| A3   | 说明 Key 通用即可                     | Keys 页增加简短说明：同一 Key 可调用账户可用模型，请求中填写模型 ID；余额和 Key 限额仍适用。展示系统配置的 API Base URL，支持复制。      |
| A4   | CC Switch 是使用问题                  | 不作功能修改；撤回上一轮未提交的后端模型发现改动。                                                                                       |
| U1   | 可保留箭头或使用详情图标              | 保留原箭头，添加“查看详情 / View details”悬停和无障碍提示。                                                                              |

仅修改前端入口与说明，不修改 Key 创建流程、模型绑定规则或模型发现接口。保持现有页面英文文案，首页提示沿用中英文切换。

其他项目等待用户逐项给意见。上一轮提前实现的改动已移出当前代码，并保存至后端仓库 `output/feedback-draft-2026-09-11/`，供后续讨论后取用。

验证：本轮生产构建通过；最终代码 TypeScript 检查通过，定向 lint 无错误（模型详情文件存在原有未使用变量警告）。构建期间本地后端不可用，出现 `ECONNREFUSED` 提示；未执行真实账户登录及线上联调。后端已恢复至本轮开始前的已提交代码。未提交、未部署。

## 2026-09-11：客户端快捷配置

按用户后续意见，参考 New API 的客户端入口设计，Keys 桌面列表与移动卡片新增 **Set up client**：

- CC Switch：Claude Code、Codex、Gemini CLI；支持模型搜索/手填及 Claude 可选模型映射。
- Cherry Studio：导入 LinkInfra OpenAI 兼容服务商，带入地址和 Key。
- Chatbox / Other：按字段复制 Base URL、Key、Model ID，提供手动配置步骤。

Key 默认隐藏，含 Key 的导入链接仅在点击打开客户端时生成；不写本地缓存，不通过第三方网页转发。不改变后端模型列表或现有客户端配置。

导入格式 4 项回归测试通过，覆盖路径、特殊字符、Unicode、缺失参数。定向 lint 无警告；浏览器使用虚构 Key 验证 1440px、390px、320px 布局、复制、应用切换、失败重试、手填模型、失效 Key 拦截，无页面错误。未启动本机客户端执行真实导入，需用户在已安装的客户端完成确认。

方案与协议参考见 [客户端配置计划](plans/2026-09-11-client-setup.md)。预览服务继续运行于 `http://localhost:3001`。

## 2026-09-11：第二批 U2–U6

用户确认 U2 已修复，U3 各板块服务不同需求，保留所有原有功能与侧栏入口。

- U4：View 列选择菜单按视口限制高度并内部滚动；长字段可换行，勾选功能保留。
- U5：头像菜单 Settings 指向 `/dashboard/setting/updateUser`，普通用户进入个人设置。
- U6：Statistics 日期、图表粒度及其他筛选自动查询，输入有 250ms 防抖；终止旧请求，避免结果倒退。缺失/倒置日期及查询错误明确提示，不展示旧数据，支持重置与重试。
- Usage 看逐条请求、费用和实时用量；Statistics 看所选范围的请求量、成功率、耗时与速度。保留两个入口并互相链接。Statistics 移除不受范围影响的实时卡片，图表跨天标签包含日期，日期弹层适配短窗口和手机。

浏览器使用模拟会话和接口验证时间/粒度自动刷新、重置、请求竞争、失败重试、非法/清空区间、管理员/普通用户接口、Settings 目标、View 菜单边界/滚动/勾选，以及 390px/320px 布局，无页面错误。未写入业务数据。

本批 TypeScript、定向 lint、生产构建通过。改动待预览，未提交或推送。详见 [第二批修复计划](plans/2026-09-11-usage-statistics-fixes.md)。
