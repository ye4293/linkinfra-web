# 每日模型排行榜

- 新增公开 `/rankings` 页面与导航入口：Top 10 + Others 堆叠趋势、近 1/7/30 天排行、Token 份额及环比。
- 三个榜单一次加载，周期切换、展开更多不追加请求；公开代理传递 ETag 和 Cache-Control。
- 显示统计截止日、历史覆盖不足、数据延迟、准备中、暂停及请求失败状态。支持中英文与移动端布局。
- 复用模型广场页头、Recharts 和模型详情链接，统计来自后端每日快照。
- 后端须同时部署 `/api/rankings`，使用已有 `NEXT_PUBLIC_API_BASE_URL` 配置。首次完整日数据生成前显示准备中。
- 验证：TypeScript 与生产构建；浏览器模拟数据验证桌面、390px、320px，周期切换不重复请求，展开更多、准备中、失败重试和无横向溢出。

本次未部署。
